import 'package:flutter/material.dart';

class AppColors {
  // Navigator theme — orange/brown
  static const Color primary = Color(0xFFC0713A);       // warm brown-orange
  static const Color primaryDark = Color(0xFFA05A2C);   // darker variant
  static const Color accent = Color(0xFFE8943A);        // bright orange
  static const Color background = Color(0xFFF7F3EF);    // warm off-white
  static const Color cardBackground = Colors.white;
  static const Color textPrimary = Color(0xFF2D2D2D);
  static const Color textSecondary = Color(0xFF6B6B6B);
  static const Color textOnPrimary = Colors.white;
  static const Color divider = Color(0xFFE0D6CC);
}
